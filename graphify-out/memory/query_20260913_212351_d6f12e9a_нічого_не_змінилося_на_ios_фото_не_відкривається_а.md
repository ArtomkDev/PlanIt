---
type: "query"
date: "2026-09-13T21:23:51.218508+00:00"
question: "нічого не змінилося на IOS фото не відкривається а просто чорний екран"
contributor: "graphify"
outcome: "corrected"
correction: "The decoder-only and animated-wrapper change was insufficient. On iOS the working StagedAttachmentImage must render outside the Reanimated, FlatList, and GestureDetector gallery stack."
source_nodes: ["AttachmentImagePreview()", "StagedAttachmentImage()", "ZoomableImagePage()"]
---

# Q: нічого не змінилося на IOS фото не відкривається а просто чорний екран

## Answer

Expanded tokens: attachment, image, preview, ios, webp, uri, staged, flat, list, gesture, animated, modal. The user confirmed the previous decoder/wrapper adjustment did not change iOS. Corrected diagnosis: the full-size image must bypass ZoomableImagePage, the horizontal FlatList, Reanimated transforms, and GestureDetector on iOS. The new iOS path renders activeUri with the already-working StagedAttachmentImage inside a native zoomable ScrollView with explicit bounds; Android and web retain the existing gallery stack.

## Outcome

- Signal: corrected
- Correction: The decoder-only and animated-wrapper change was insufficient. On iOS the working StagedAttachmentImage must render outside the Reanimated, FlatList, and GestureDetector gallery stack.

## Source Nodes

- AttachmentImagePreview()
- StagedAttachmentImage()
- ZoomableImagePage()